import * as React from 'react';

class MissionViewer extends React.Component<void, void> {
  render() {
    return (
      <div className="mission-viewer">TODO: run a mission and see the outcome</div>
    );
  }
}

export default MissionViewer;
